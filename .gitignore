# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
*.swp

# Node (none used yet; keep clean if tooling is added later)
node_modules/

# Wrangler (Cloudflare Workers) local build/dev cache - regenerated on every
# `wrangler deploy`/`dev`; contains account metadata, not meant to be shared
.wrangler/

# Claude Code machine-local settings (permissions allowlist) — not shared
.claude/settings.local.json

# Personal research/case-study notes — not part of the shipped project
case-studies/

# Committed on purpose (do NOT ignore): .claude/agents/, each game's vendor/phaser.min.js,
# and snapshots/ — the games must stay self-contained and the snapshot history is the project.
